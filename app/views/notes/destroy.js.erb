$(function(){
  $('#sequence-info').text('');
  '<% @sequence.notes.each do |note| %>'
    $('#sequence-info').append("<%= j link_to "#{note.value}", note, method: 'delete', remote:true %> ");
  '<% end %>'
})
